"""Accuracy/correctness benchmarks (eval suite).

Distinct from the perf benches in the parent ``benchmarks/`` directory.
Perf benches measure ops/sec; eval suites measure recall, precision,
false-merge rate, calibration, and other correctness metrics intended
for peer review.

Currently houses:
  - ess_accuracy/ — Entity Sense Signature resolution accuracy
"""
