# Eval results are timestamped per-run artifacts — not source.
# Commit specific runs deliberately when worth pinning (e.g. "the
# numbers behind the v0.7.2 release"); ignore by default.
*
!.gitignore
