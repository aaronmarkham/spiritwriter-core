# Backlog

We decided to use a rate limiter because the public API is getting abused.
Always validate webhook signatures before processing.

Add OAuth login with Google — tracked in TICKET-412.
Migrate the worker queue off Redis — tracked in TICKET-419.
